/**
 * @license
 * Copyright 2019 Ludan Stoecklé
 * SPDX-License-Identifier: Apache-2.0
 */
import { GenderNumberManager } from 'rosaenlg/dist/GenderNumberManager';
import { SynManager } from 'rosaenlg/dist/SynManager';
import { LanguageImpl } from 'rosaenlg/dist/LanguageImpl';
import { VerbsData } from 'rosaenlg-pug-code-gen';
export interface ConjParams {
    verb: string;
    pronominal?: boolean;
    tense?: string;
}
export declare type VerbParts = string[];
export declare class VerbsManager {
    private languageImpl;
    private genderNumberManager;
    private synManager;
    private spy;
    private embeddedVerbs;
    private verbParts;
    constructor(languageImpl: LanguageImpl, genderNumberManager: GenderNumberManager, synManager: SynManager);
    getVerbPartsList(): VerbParts;
    setVerbPartsList(verbParts: VerbParts): void;
    setEmbeddedVerbs(embeddedVerbs: VerbsData): void;
    setSpy(spy: Spy): void;
    private encapsulateConjParams;
    getAgreeVerb(subject: any, conjParamsOriginal: string | ConjParams, additionalParams: any): string;
    popVerbPart(): string;
}
